describe("useApi hook", () => {
  test.todo("fetches the api response when fetch() is called");
  test.todo("fetches the correct url (base url plus path)");
  test.todo("returns loading when loading");
  test.todo("returns response when api response is ok");
  test.todo("returns error when api response is not ok");
});
